spring:
  datasource:
    url: jdbc:mysql://localhost:3306/scia435web?characterEncoding=UTF-8
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false